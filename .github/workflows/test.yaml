name: 'Test Workflow'
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

jobs:
  test:
    name: 'Simple Test'
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Cowsay Hello'
        run: |
          sudo apt-get update
          sudo apt-get install -y cowsay
          /usr/games/cowsay "Hello from test workflow!"
          echo "Test workflow completed successfully!"